pipeline {
    agent any
    stages{
        stage('Build'){
            steps {
                sh '/usr/bin/mvn clean package'
            }
            post {
                success {
                    echo 'Now Archiving...'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }
}
